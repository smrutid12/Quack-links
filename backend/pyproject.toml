[project]
name = "quacklinks-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "Flask==3.0.3",
  "Flask-Cors==4.0.1",
  "Flask-RESTX==1.3.0",
  "Flask-SQLAlchemy==3.1.1",
  "SQLAlchemy==2.0.36",
  "psycopg2-binary==2.9.10",
  "python-dotenv==1.0.1"
]

[project.scripts]
app = "server:app"